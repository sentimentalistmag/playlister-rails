<%= form_for(@artist) do |f| %>

// form_tag

  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :record_label %><br>
    <%= f.text_field :record_label %>
    
    <input type='text' id='artist_record_lable' name='artist[record_label]' value='<%=@artist.record_label%>'/>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
